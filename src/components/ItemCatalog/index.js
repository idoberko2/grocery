import React from 'react';
import { StyleSheet, View, ListView, Text } from 'react-native';

export default ItemCatalog = ({ navigation }) => {
    console.log({ navigation });

    return <Text>Mush catalog</Text>;
};
